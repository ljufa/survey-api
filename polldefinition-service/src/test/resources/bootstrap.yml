spring:
  data:
    mongodb:
      host: localhost
      username: user
      password: password
      database: survey
      port: 27017
eureka:
  client:
    enabled: false

logging:
  level:
    com.draganlj.survey: DEBUG