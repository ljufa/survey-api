spring:
  application:
    name: polldefinition-service
  data:
    mongodb:
      host: polldefinition-mongodb
      username: user
      password: password
      database: survey
      port: 27017
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://survey-registry:8761/eureka/

server:
  context-path: /definition
  port: 7878
